import React from "react";
import Navigator from "../components/Navigator";

function Dashboard() {
    return (
        <div>
            < Navigator />
        </div>
    );
}

export default Dashboard;